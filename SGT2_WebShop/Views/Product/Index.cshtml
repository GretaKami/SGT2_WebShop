@model List<ProductModel>;
<h1>Products</h1>
<table class="table">
    <tr>
        <th>Category</th>
        <th>Subcategory</th>
        <th>Name</th>
        <th>Description</th>
        <th>Price</th>
        
    </tr>
    @foreach (var product in Model)
    {
        <tr>
            <td>@product.Subcategory.Category.Title</td>
            <td>@product.Subcategory.Title</td>
            <td><a asp-area="" asp-controller="Product" asp-action="View" asp-route-id="@product.Id">@product.Name</a></td>
            <td>@product.Description</td>
            <td>@product.Price</td> 
        </tr>
    }
</table>